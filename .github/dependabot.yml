version: 2
updates:
  # Keeps this repository's OWN workflows (.github/workflows/*.yml) current -
  # it understands the `@<sha> # v1.2.3` pin convention used throughout and
  # opens a PR bumping both when a pinned action releases.
  #
  # This does NOT reach the two workflow templates under
  # skills/lokf-scaffolding/templates/github/ - Dependabot's github-actions
  # ecosystem only scans .github/workflows/ (or a root action.yml), a known
  # limitation (dependabot/dependabot-core#5970). Those pins still need a
  # manual bump; lint-workflows' actionlint step catches syntax drift, not
  # staleness. Re-evaluate if that upstream limitation is ever lifted.
  #
  # This repository dogfoods both templates, so each one is currently kept
  # byte-identical to its live counterpart above. That makes the manual bump a
  # copy rather than a judgement call, and staleness cheap to spot:
  #
  #   for f in knowledge-validate knowledge-librarian; do
  #     diff "skills/lokf-scaffolding/templates/github/$f.yaml" \
  #          ".github/workflows/$f.yaml"
  #   done
  #
  # Deliberate divergence is allowed - if a template ever needs to differ from
  # the dogfooded copy, say why here so the diff above isn't read as a defect.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "chore(actions)"
